import React from 'react';
import PlaceholderChart from './PlaceholderChart';

const AreaChart = (props) => {
  return <PlaceholderChart type="area" title="Area Chart" {...props} />;
};

export default AreaChart;