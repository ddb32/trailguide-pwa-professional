import React from 'react';
import PlaceholderChart from './PlaceholderChart';

const BarChart = (props) => {
  return <PlaceholderChart type="bar" title="Bar Chart" {...props} />;
};

export default BarChart;