import React from 'react';
import PlaceholderChart from './PlaceholderChart';

const PieChart = (props) => {
  return <PlaceholderChart type="pie" title="Pie Chart" {...props} />;
};

export default PieChart;