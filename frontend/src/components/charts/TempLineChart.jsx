import React from 'react';
import PlaceholderChart from './PlaceholderChart';

const LineChart = (props) => {
  return <PlaceholderChart type="line" title="Line Chart" {...props} />;
};

export default LineChart;